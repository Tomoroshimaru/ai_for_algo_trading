# `qc`

Quality-control checks on the analytics outputs (coverage, sanity, arbitrage) feeding the validation layer.

## Modules
- `quality.py`

See `docs/INTERFACE_CONTRACTS.md` for frozen signatures and
`docs/RUNBOOKS.md` for operational procedures.
