# `risk`

Greeks computation and risk aggregation (by underlying/expiry/portfolio) plus broker reconciliation.

## Modules
- `analytics.py`

See `docs/INTERFACE_CONTRACTS.md` for frozen signatures and
`docs/RUNBOOKS.md` for operational procedures.
