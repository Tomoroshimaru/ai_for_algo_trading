# `snapshots`

Normalized market-state builders: turn raw events into a clean, staleness-aware snapshot (market_state).

## Modules
- `builder.py`

See `docs/INTERFACE_CONTRACTS.md` for frozen signatures and
`docs/RUNBOOKS.md` for operational procedures.
