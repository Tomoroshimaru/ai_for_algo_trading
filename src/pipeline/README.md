# `pipeline`

Daily orchestration glue: run_day() chains forwards -> IV -> surface -> pricing/risk -> scenarios into one RunResult.

## Modules
- `daily.py`

See `docs/INTERFACE_CONTRACTS.md` for frozen signatures and
`docs/RUNBOOKS.md` for operational procedures.
