# `connectivity`

IBKR session management: connect/reconnect with backoff, health & clock-skew probes, diagnostics evidence for the bootstrap smoke test.

## Modules
- `diagnostics.py`
- `session.py`

See `docs/INTERFACE_CONTRACTS.md` for frozen signatures and
`docs/RUNBOOKS.md` for operational procedures.
