# `storage`

Partitioned Parquet store (single-partition overwrite, idempotent) and the frozen dataset schemas (single source of truth).

## Modules
- `metadata.py`
- `parquet_store.py`
- `schemas.py`

See `docs/INTERFACE_CONTRACTS.md` for frozen signatures and
`docs/RUNBOOKS.md` for operational procedures.
