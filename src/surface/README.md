# `surface`

Volatility surface fitting (per-expiry smile parametrisation) and arbitrage-aware grid sampling.

## Modules
- `engine.py`

See `docs/INTERFACE_CONTRACTS.md` for frozen signatures and
`docs/RUNBOOKS.md` for operational procedures.
